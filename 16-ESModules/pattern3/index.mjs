import math from './pattern3.mjs' //since this is default import it can be imported with any name

const { add, subtract } = math;

console.log("sum = ", add(2, 4));

console.log("difference = ", subtract(2, 4));
